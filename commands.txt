Step 1 -> Intialise a GIT Repo using git init command
Step 2 -> To add a file to staging area we use the git add command
Step 3 -> To commit changes we use the git commit command. This will let us track the files.
